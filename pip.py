# pip is a package manager for python . You can download any package related to the python .
# Module is a code written by someone else (Usually). There is generally two types of module : 
# Buildin module (preinstalled in python)
# External module(need to install using pip )

import pyjokes
print(pyjokes.get_joke())